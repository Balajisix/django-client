import { PlaceholderPage } from "../../components/layout/PlaceholderPage";

export function MyClaimsPage() {
  return (
    <PlaceholderPage
      title="My Claims"
      description="Create a claim, track its status, and upload required documents."
      stageNote="Built in Stage 3 — customer portal."
    />
  );
}
