import { PlaceholderPage } from "../../components/layout/PlaceholderPage";

export function CustomersPage() {
  return (
    <PlaceholderPage
      title="Customers"
      description="Browse customer profiles, their policies and claim history."
      stageNote="Built in Stage 5 — manager view."
    />
  );
}
