import { PlaceholderPage } from "../../components/layout/PlaceholderPage";

export function ClaimsQueuePage() {
  return (
    <PlaceholderPage
      title="Claims Queue"
      description="Review claim documents, AI intelligence, and move claims through the workflow."
      stageNote="Built in Stage 4 — claims officer workspace."
    />
  );
}
